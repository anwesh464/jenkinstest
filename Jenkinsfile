node {
  stage('Checkout') {
    checkout scm
  }
  stage('Build') {
    echo 'Building....${env.BUILD_ID}'
  }
  stage('Test') {
      echo 'Testing....'
  }
  stage('Deploy') {
      echo 'Deploying....'
  }
}
